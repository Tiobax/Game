#pragma once
#include <string>

const int SCREEN_WIDTH = 1500;
const int SCREEN_HEIGHT = 900;
const float BALL_SPEED = 700.f;
const float BALL_RADIUS = 10.f;
const int PLATFORM_LENGTH = 150;
const int PLATFORM_WIDTH = 20;
const int PLATFORM_SHIFT = 5;
const float PLATFORM_SPEED = 550.f;
const std::string RESOURCES_PATH = "Resources/";
const float TEXT_SIZE_PERCENT = 0.05f;
const int VICTORY_SCORE = 5;
const float PLATFORM_AI_SPEED = 350.f;
const int BUTTON_WIDTH = 300;
const int BUTTON_HEIGHT = 70;
const float UP_BOARD_POSITION = SCREEN_HEIGHT * TEXT_SIZE_PERCENT + SCREEN_HEIGHT * TEXT_SIZE_PERCENT * TEXT_SIZE_PERCENT * 2 + 2;
